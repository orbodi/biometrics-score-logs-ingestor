DB_HOST=localhost
DB_PORT=5432
DB_NAME=biometrics
DB_USER=biometrics_user
DB_PASSWORD=change_me

LOG_LEVEL=INFO

# Répertoire local où déposer les fichiers .log collectés
INPUT_DIR=inputs

# Répertoire de sortie des fichiers JSON générés
OUTPUT_JSON_DIR=outputs

# Répertoire d'archivage des fichiers .log déjà traités
ARCHIVE_DIR=archive

# Fichier JSON décrivant les serveurs SSH (nom, IP/host, remote_dir)
# Voir l'exemple dans `servers.example.json`
SSH_SERVERS_FILE=servers.example.json

# Identifiants SSH utilisés pour se connecter aux serveurs
SSH_USER=your_ssh_username
SSH_PASSWORD=your_ssh_password

# Timeout pour les connexions SSH (en secondes, défaut: 30)
SSH_TIMEOUT=30
